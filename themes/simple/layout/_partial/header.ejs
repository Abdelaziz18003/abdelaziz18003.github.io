<header>
  <nav class="navbar">
    <ul>
      <% for (let i in theme.menu) { %>
        <li><a href="<%- url_for(theme.menu[i]) %>" class="ml-md"><%= i %></a></li>
      <% } %>
    </ul>
  </nav>
</header>